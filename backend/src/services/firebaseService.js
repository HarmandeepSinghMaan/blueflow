// Placeholder service for Firebase interactions from backend (if needed)
export const sendEmailVerificationLink = async (email) => {
  return true
}

export const verifyEmailLink = async (token) => {
  return true
}

export const sendMobileOtp = async (mobileNo) => {
  return true
}

export const verifyMobileOtp = async (mobileNo, otp) => {
  return true
}